---
description: 博客仓库通用协作与修改边界
alwaysApply: true
---

# 项目通用规则

- 项目类型：VitePress 文档站，优先修改 `docs/` 与 `docs/.vitepress/`。
- 格式化规范：遵循 `.prettierrc.json`，修改后优先执行局部 prettier。
- 生成产物：`docs/.vitepress/dist/**` 为构建产物，除非用户明确要求，不要手动编辑或纳入提交。
- 路由改动：新增、移动、重命名文档后，同步更新 `docs/.vitepress/config/routes.ts`。
- 命名风格：文档导航与正文中的英文术语优先使用小写或小写驼峰，保持全站一致。
- 修改原则：优先小步、可验证改动；不要无关重构，不要引入与任务无关的风格漂移。
