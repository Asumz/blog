---
description: VitePress 路由与侧边栏命名规范
globs: docs/.vitepress/config/*.ts
alwaysApply: false
---

# 路由配置规则

- 导航分组名称与目录语义一致，避免“目录已改名但分组名未同步”。
- 文档目录移动后，及时同步 `base` 路径与 `items.link`。
- 侧边栏文案遵循项目术语风格：英文术语优先小写或小写驼峰。
- 展示名仅改文案，不改 `link` slug；若改 slug，必须同步文件路径与引用。
- 调整顺序时优先“基础概念 -> 工程实践 -> 手写/实现”。
